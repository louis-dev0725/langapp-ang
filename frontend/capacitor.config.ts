import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.langapp.app',
  appName: 'langapp',
  webDir: 'dist/langapp',
  bundledWebRuntime: false,
};

export default config;
